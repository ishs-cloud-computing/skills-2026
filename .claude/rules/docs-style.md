---
paths:
  - "set-*/task-*/**/*.md"
  - "task-3/**/*.md"
  - "shared/**/*.md"
---

문서 종류를 먼저 정하고 **한 파일에 둘 이상 섞지 않는다.** 같은 정보를 두 곳에 쓰지 않는다.

| 파일 | 담는 것 | 금지 |
| --- | --- | --- |
| `README.md` | 실행·배포·teardown 절차. 명령형 단계 | **"왜" 를 쓰지 않는다** |
| `NOTES.md` | 함정·기각 대안·삽질·실측 소요시간·결정/정정 로그·채점 커버리지 | 절차 재기술 |
| `ARCHITECTURE.md` | 설계 이유·트레이드오프 | 명령·절차 |

전체 기준: [`.claude/context/layout.md`](../context/layout.md#문서-삼분할).
`docs/` 는 이 저장소에 없다 — 새로 만들지 않는다.
