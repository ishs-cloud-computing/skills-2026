# book Service (요구사항 8: Service Name book-svc)
apiVersion: v1
kind: Service
metadata:
  name: book-svc
  namespace: skills
spec:
  type: ClusterIP
  selector:
    app: book
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
