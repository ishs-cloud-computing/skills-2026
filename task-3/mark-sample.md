# 2026년도 전국기능경기대회 채점기준 — 제3과제

**직종명**: 클라우드컴퓨팅

## 1. 채점상의 유의사항

※ 다음 사항을 유의하여 채점하시오.

1. AWS의 지역은 `ap-northeast-2`을 사용합니다.
2. 웹페이지 접근은 크롬이나 파이어폭스를 이용합니다.
3. 웹페이지에서 언어에 따라 문구가 다르게 보일 수 있습니다.
4. shell에서의 명령어의 출력은 버전에 따라 조금 다를 수 있습니다.
5. 문제지와 채점지에 있는 `<>` 는 변수입니다. 해당 부분을 변경해 입력합니다.
6. 채점은 문항 순서대로 진행해야 합니다.
7. 삭제된 채점자료는 되돌릴 수 없음으로 유의하여 진행하며, 이의신청까지 완료 이후 선수가 생성한 클라우드 리소스를 삭제합니다.
8. 부분 점수가 있는 문항은 채점 항목에 부분 점수가 적혀져 있습니다.
9. 부분 점수가 따로 없는 문항은 모두 맞아야 점수로 인정됩니다.
10. 리소스의 정보를 읽어오는 채점항목은 기본적으로 스크립트 결과를 통해 채점을 진행하며, 만약 선수가 이의가 있다면 명령어를 직접 입력하여 확인해볼 수 있습니다.
11. `[ ]` 기호는 채점에 영향을 주지 않습니다.
12. 채점 내용의 `$` 기호는 명령어에 포함되는 것이 아니라 쉘을 의미합니다.

## 2. 채점기준표

### 1) 주요항목별 배점

| 과제번호 | 일련번호 | 주요항목 | 배점 | 채점방법 (독립) | 채점방법 (합의) | 채점시기 (경기진행중) | 채점시기 (경기종료후) |
|---|---|---|---|---|---|---|---|
| 제3과제 | 1 | 비정상 요청 처리 | 4 | ○ |  |  | ○ |
| 제3과제 | 2 | 고가용성 및 안정성 | 12 | ○ |  |  | ○ |
| 제3과제 | 3 | 성능 효율성 | 12 | ○ |  |  | ○ |
| 제3과제 | 4 | 비용 최적화 | 12 | ○ |  |  | ○ |
| | | **합계** | **40** | | | | |

### 2) 채점방법 및 기준

#### 주요항목 1 — 비정상 요청 처리

| 일련번호 | 세부항목(채점방법) | 배점 |
|---|---|---|
| 1 | Image 처리율 >= 90% | 0.5 |
| 2 | Image 처리율 >= 85% | 0.5 |
| 3 | Image 처리율 >= 80% | 0.5 |
| 4 | Image 처리율 >= 50% | 0.5 |
| 5 | 비정상 요청 처리율 >= 90% | 0.5 |
| 6 | 비정상 요청 처리율 >= 85% | 0.5 |
| 7 | 비정상 요청 처리율 >= 80% | 0.5 |
| 8 | 비정상 요청 처리율 >= 50% | 0.5 |

#### 주요항목 2 — 고가용성 및 안정성

| 일련번호 | 세부항목(채점방법) | 배점 |
|---|---|---|
| 1 | user API 로드 처리 >= 90.0% | 0.5 |
| 2 | user API 로드 처리 >= 87.5% | 0.5 |
| 3 | user API 로드 처리 >= 85.0% | 0.5 |
| 4 | user API 로드 처리 >= 82.5% | 0.5 |
| 5 | user API 로드 처리 >= 80.0% | 0.5 |
| 6 | user API 로드 처리 >= 70.0% | 0.5 |
| 7 | user API 로드 처리 >= 50.0% | 0.5 |
| 8 | user API 로드 처리 >= 30.0% | 0.5 |
| 9 | product API 로드 처리 >= 90.0% | 0.5 |
| 10 | product API 로드 처리 >= 87.5% | 0.5 |
| 11 | product API 로드 처리 >= 85.0% | 0.5 |
| 12 | product API 로드 처리 >= 82.5% | 0.5 |
| 13 | product API 로드 처리 >= 80.0% | 0.5 |
| 14 | product API 로드 처리 >= 70.0% | 0.5 |
| 15 | product API 로드 처리 >= 50.0% | 0.5 |
| 16 | product API 로드 처리 >= 30.0% | 0.5 |
| 17 | stress API 로드 처리 >= 90.0% | 0.5 |
| 18 | stress API 로드 처리 >= 87.5% | 0.5 |
| 19 | stress API 로드 처리 >= 85.0% | 0.5 |
| 20 | stress API 로드 처리 >= 82.5% | 0.5 |
| 21 | stress API 로드 처리 >= 80.0% | 0.5 |
| 22 | stress API 로드 처리 >= 70.0% | 0.5 |
| 23 | stress API 로드 처리 >= 50.0% | 0.5 |
| 24 | stress API 로드 처리 >= 30.0% | 0.5 |

#### 주요항목 3 — 성능 효율성

| 일련번호 | 세부항목(채점방법) | 배점 |
|---|---|---|
| 1 | (user API 로드 처리 <= 0.2 sec) >= 90.0% | 0.5 |
| 2 | (user API 로드 처리 <= 0.2 sec) >= 87.5% | 0.5 |
| 3 | (user API 로드 처리 <= 0.2 sec) >= 85.0% | 0.5 |
| 4 | (user API 로드 처리 <= 0.2 sec) >= 82.5% | 0.5 |
| 5 | (user API 로드 처리 <= 0.2 sec) >= 80.0% | 0.5 |
| 6 | (user API 로드 처리 <= 0.2 sec) >= 70.0% | 0.5 |
| 7 | (user API 로드 처리 <= 0.2 sec) >= 50.0% | 0.5 |
| 8 | (user API 로드 처리 <= 0.2 sec) >= 30.0% | 0.5 |
| 9 | (product API 로드 처리 <= 0.2 sec) >= 90.0% | 0.5 |
| 10 | (product API 로드 처리 <= 0.2 sec) >= 87.5% | 0.5 |
| 11 | (product API 로드 처리 <= 0.2 sec) >= 85.0% | 0.5 |
| 12 | (product API 로드 처리 <= 0.2 sec) >= 82.5% | 0.5 |
| 13 | (product API 로드 처리 <= 0.2 sec) >= 80.0% | 0.5 |
| 14 | (product API 로드 처리 <= 0.2 sec) >= 70.0% | 0.5 |
| 15 | (product API 로드 처리 <= 0.2 sec) >= 50.0% | 0.5 |
| 16 | (product API 로드 처리 <= 0.2 sec) >= 30.0% | 0.5 |
| 17 | (stress API 로드 처리 <= 1.0 sec) >= 90.0% | 0.5 |
| 18 | (stress API 로드 처리 <= 1.0 sec) >= 87.5% | 0.5 |
| 19 | (stress API 로드 처리 <= 1.0 sec) >= 85.0% | 0.5 |
| 20 | (stress API 로드 처리 <= 1.0 sec) >= 82.5% | 0.5 |
| 21 | (stress API 로드 처리 <= 1.0 sec) >= 80.0% | 0.5 |
| 22 | (stress API 로드 처리 <= 1.0 sec) >= 70.0% | 0.5 |
| 23 | (stress API 로드 처리 <= 1.0 sec) >= 50.0% | 0.5 |
| 24 | (stress API 로드 처리 <= 1.0 sec) >= 30.0% | 0.5 |

#### 주요항목 4 — 비용 최적화

| 일련번호 | 세부항목(채점방법) | 배점 |
|---|---|---|
| 1 | 0.5 <= 인스턴스 비용 ratio <= 1.00 | 1.0 |
| 2 | 0.5 <= 인스턴스 비용 ratio <= 1.25 | 1.0 |
| 3 | 0.5 <= 인스턴스 비용 ratio <= 1.50 | 1.0 |
| 4 | 0.5 <= 인스턴스 비용 ratio <= 1.75 | 1.0 |
| 5 | 0.5 <= 인스턴스 비용 ratio <= 2.00 | 1.0 |
| 6 | 0.5 <= 인스턴스 비용 ratio <= 2.25 | 1.0 |
| 7 | 0.5 <= 인스턴스 비용 ratio <= 2.50 | 1.0 |
| 8 | 0.5 <= 인스턴스 비용 ratio <= 2.75 | 1.0 |
| 9 | 0.5 <= 인스턴스 비용 ratio <= 3.00 | 1.0 |
| 10 | 0.5 <= 인스턴스 비용 ratio <= 3.25 | 1.0 |
| 11 | 0.5 <= 인스턴스 비용 ratio <= 3.50 | 1.0 |
| 12 | 0.5 <= 인스턴스 비용 ratio <= 3.75 | 1.0 |

**총점: 40**

### 3) 채점내용

#### 순번 0 — 사전준비

1. 선수가 제출한 엔드포인트가 선수의 시스템이 맞는지 확인하고 틀리다면 모든 항목 0점 처리 합니다.
2. DB 타입 및 대수를 확인하고 다른 형태 및 문제 의도와 다르게 사용 시 "3. 성능 효율성", "4. 비용 최적화" 항목을 모두 0점 처리합니다.
3. 기타 EC2 및 리소스 상태를 점검하고 다른 형태를 사용 시 채점항목에 0점을 부여할 수 있습니다.
4. Lambda 사용과 관련해 부적절한 형태로 사용 시 전체 0점 처리합니다.

#### 채점 항목

모든 항목은 다음 절차로 진행합니다:
1. SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)
2. `results_<비번호>.log` 파일을 확인하여 아래 기준값을 확인합니다.

**비정상 요청 처리 (1-1 ~ 1-8)**

| 순번 | 확인 값 |
|---|---|
| 1-1 | image download 값이 90.0% 이상 |
| 1-2 | image download 값이 85.0% 이상 |
| 1-3 | image download 값이 80.0% 이상 |
| 1-4 | image download 값이 50.0% 이상 |
| 1-5 | Exception Handling 값이 90.0% 이상 |
| 1-6 | Exception Handling 값이 85.0% 이상 |
| 1-7 | Exception Handling 값이 80.0% 이상 |
| 1-8 | Exception Handling 값이 50.0% 이상 |

**고가용성 및 안정성 — availability (2-1 ~ 2-24)**

| 순번 | 확인 값 |
|---|---|
| 2-1 | (user) availability 값이 90.0% 이상 |
| 2-2 | (user) availability 값이 87.5% 이상 |
| 2-3 | (user) availability 값이 85.0% 이상 |
| 2-4 | (user) availability 값이 82.5% 이상 |
| 2-5 | (user) availability 값이 80.0% 이상 |
| 2-6 | (user) availability 값이 70.0% 이상 |
| 2-7 | (user) availability 값이 50.0% 이상 |
| 2-8 | (user) availability 값이 30.0% 이상 |
| 2-9 | (product) availability 값이 90.0% 이상 |
| 2-10 | (product) availability 값이 87.5% 이상 |
| 2-11 | (product) availability 값이 85.0% 이상 |
| 2-12 | (product) availability 값이 82.5% 이상 |
| 2-13 | (product) availability 값이 80.0% 이상 |
| 2-14 | (product) availability 값이 70.0% 이상 |
| 2-15 | (product) availability 값이 50.0% 이상 |
| 2-16 | (product) availability 값이 30.0% 이상 |
| 2-17 | (stress) availability 값이 90.0% 이상 |
| 2-18 | (stress) availability 값이 87.5% 이상 |
| 2-19 | (stress) availability 값이 85.0% 이상 |
| 2-20 | (stress) availability 값이 82.5% 이상 |
| 2-21 | (stress) availability 값이 80.0% 이상 |
| 2-22 | (stress) availability 값이 70.0% 이상 |
| 2-23 | (stress) availability 값이 50.0% 이상 |
| 2-24 | (stress) availability 값이 30.0% 이상 |

**성능 효율성 — performance (3-1 ~ 3-24)**

| 순번 | 확인 값 |
|---|---|
| 3-1 | (user) performance 값이 90.0% 이상 |
| 3-2 | (user) performance 값이 87.5% 이상 |
| 3-3 | (user) performance 값이 85.0% 이상 |
| 3-4 | (user) performance 값이 82.5% 이상 |
| 3-5 | (user) performance 값이 80.0% 이상 |
| 3-6 | (user) performance 값이 70.0% 이상 |
| 3-7 | (user) performance 값이 50.0% 이상 |
| 3-8 | (user) performance 값이 30.0% 이상 |
| 3-9 | (product) performance 값이 90.0% 이상 |
| 3-10 | (product) performance 값이 87.5% 이상 |
| 3-11 | (product) performance 값이 85.0% 이상 |
| 3-12 | (product) performance 값이 82.5% 이상 |
| 3-13 | (product) performance 값이 80.0% 이상 |
| 3-14 | (product) performance 값이 70.0% 이상 |
| 3-15 | (product) performance 값이 50.0% 이상 |
| 3-16 | (product) performance 값이 30.0% 이상 |
| 3-17 | (product) performance 값이 90.0% 이상 *(원문 표기 그대로)* |
| 3-18 | (stress) performance 값이 87.5% 이상 |
| 3-19 | (stress) performance 값이 85.0% 이상 |
| 3-20 | (stress) performance 값이 82.5% 이상 |
| 3-21 | (stress) performance 값이 80.0% 이상 |
| 3-22 | (stress) performance 값이 70.0% 이상 |
| 3-23 | (stress) performance 값이 50.0% 이상 |
| 3-24 | (stress) performance 값이 30.0% 이상 |

**비용 최적화 — cost ratio (4-1 ~ 4-12)**

각 항목은 다음을 모두 확인합니다:
1. cost ratio 값이 아래 범위 이내인지 확인
2. (user) performance 값이 30% 이상
3. (product) performance 값이 30% 이상
4. (stress) performance 값이 30% 이상

| 순번 | cost ratio 범위 |
|---|---|
| 4-1 | 0.50 이상, 1.00 이하 |
| 4-2 | 0.50 이상, 1.25 이하 |
| 4-3 | 0.50 이상, 1.50 이하 |
| 4-4 | 0.50 이상, 1.75 이하 |
| 4-5 | 0.50 이상, 2.00 이하 |
| 4-6 | 0.50 이상, 2.25 이하 |
| 4-7 | 0.50 이상, 2.50 이하 |
| 4-8 | 0.50 이상, 2.75 이하 |
| 4-9 | 0.50 이상, 3.00 이하 |
| 4-10 | 0.50 이상, 3.25 이하 |
| 4-11 | 0.50 이상, 3.50 이하 |
| 4-12 | 0.50 이상, 3.75 이하 |
