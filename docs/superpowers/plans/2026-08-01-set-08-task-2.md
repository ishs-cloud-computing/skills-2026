# set-08 task-2 구현 계획 (스캐폴드 + module-2-lattice + module-4-sqs-scaling)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** set-08 task-2를 스캐폴드하고 module-2(VPC Lattice)·module-4(SQS+KEDA+Karpenter/Fargate)를 terraform/eksctl/k8s로 구현, plan 수준까지 검증.

**Architecture:** 스펙 `docs/superpowers/specs/2026-08-01-set-08-task-2-design.md` 참조. module-2는 terraform 단일 루트(VPC 2개 + Lattice SN/Service/TG/Listener + EC2 user-data 앱). module-4는 terraform(VPC/SQS/ECR/IAM) + eksctl(클러스터·Fargate profile 3개·IRSA 3개) + helm(KEDA·Karpenter) + k8s manifest(placeholder 치환).

**Tech Stack:** Terraform 1.15.8 (mise 고정), AWS provider ~> 6.56, eksctl·helm 최신 안정, KEDA 2.x, Karpenter 1.x, pdfminer.six (문서 변환)

## Global Constraints

- 모든 신규 소스 파일 상단 SPDX 헤더: `# SPDX-License-Identifier: Apache-2.0` + `# Copyright 2026 The ISHS Cloud Computing Authors`
- 리소스 이름은 과제지와 정확 일치. 채점 대상 이름·CIDR·리전·인스턴스 타입은 전부 variables.tf 변수 (기본값 = 과제지 값)
- `provided/` 원본 수정 금지 — terraform은 `file()`/`base64encode()`로 참조만
- 커밋 메시지 영어 명령형. 브랜치 `set-08/task-2` (이미 존재)
- tfstate·`.terraform/`·`outputs.json`·kubeconfig·`rendered/` 커밋 금지
- 채점 스크립트가 검사하는 필드는 중복·불필요해 보여도 제거 금지
- 과제지 확정값: module-4 `pollingInterval: 15`(≤15), `cooldownPeriod: 30`(≤30초), `PROCESSING_SECONDS=5`(초), SQS visibility `30`(≥30초), NodePool `spec.disruption.consolidationPolicy` 설정 존재 필수(값 자유), module-2 service-sg에 `0.0.0.0/0` 허용 시 **명시적 미충족**
- 자료 원본: `C:\Users\User\Downloads\national-skills-v7\2과제\`
- 검증 깊이: `terraform fmt`·`validate`까지 필수. `plan`은 AWS 자격증명 있을 때만 (없으면 해당 스텝 결과에 "credentials 없음 — plan 미실행" 기록하고 진행)

---

### Task 1: 스캐폴드

**Files:**
- Create: `set-08/task-2/` (전체 트리, `_template/task-2` 복사)
- Create: `set-08/task-2/provided/module-{1..4}/` (지급파일 복사)
- Create: `set-08/task-2/mark/mark2-{1..4}.sh` (check 스크립트 복사)
- Create: `set-08/task-2/task.pdf`, `set-08/task-2/mark.pdf`

**Interfaces:**
- Produces: 이후 모든 태스크의 디렉토리 구조. `provided/module-2/{client_app.py,service_app.py}`, `provided/module-4/worker.py`는 terraform·Dockerfile이 참조하는 정확 경로.

- [ ] **Step 1: template 복사 + 모듈 개명**

```powershell
Copy-Item -Recurse _template/task-2 set-08/task-2
Rename-Item set-08/task-2/module-2 module-2-lattice
Rename-Item set-08/task-2/module-4 module-4-sqs-scaling
# module-2-lattice: eksctl/·k8s/ 불필요 (terraform 전용 모듈) — 삭제
Remove-Item -Recurse set-08/task-2/module-2-lattice/eksctl, set-08/task-2/module-2-lattice/k8s
# module-4는 terraform/eksctl/k8s 전부 사용 + app/ 추가
New-Item -ItemType Directory set-08/task-2/module-4-sqs-scaling/app
```

- [ ] **Step 2: 지급파일·채점 스크립트·PDF 복사**

```powershell
$SRC = "C:\Users\User\Downloads\national-skills-v7\2과제"
1..4 | ForEach-Object { Copy-Item "$SRC\모듈$($_)_지급파일\*" "set-08/task-2/provided/module-$_/" }
New-Item -ItemType Directory set-08/task-2/mark -Force
1..4 | ForEach-Object { Copy-Item "$SRC\asgmt2_module$($_)_check.sh" "set-08/task-2/mark/mark2-$_.sh" }
Copy-Item "$SRC\2과제_문제지.pdf" set-08/task-2/task.pdf
Copy-Item "$SRC\2과제_채점지.pdf" set-08/task-2/mark.pdf
```

- [ ] **Step 3: 검증 — 구조·LFS**

```powershell
Get-ChildItem -Recurse set-08/task-2 | Select-Object FullName
Get-Content .gitattributes | Select-String "pdf"
```
Expected: provided 4모듈 파일 전부 존재, mark2-1~4.sh 존재, `*.pdf`(또는 `set-08/**.pdf`)가 LFS 트래킹에 포함. LFS 패턴 없으면 `git lfs track "set-08/task-2/*.pdf"` 실행 후 .gitattributes 커밋 포함.

- [ ] **Step 4: mark 스크립트 무결성 확인**

```powershell
git diff --no-index "C:\Users\User\Downloads\national-skills-v7\2과제\asgmt2_module2_check.sh" set-08/task-2/mark/mark2-2.sh
```
Expected: diff 없음 (원본 그대로, 개명만).

- [ ] **Step 5: Commit**

```powershell
git add set-08/task-2 .gitattributes
git commit -m "Scaffold set-08 task-2 with provided files and mark scripts"
```

---

### Task 2: task.md / mark.md 변환

**Files:**
- Create: `set-08/task-2/task.md`
- Create: `set-08/task-2/mark.md`

**Interfaces:**
- Consumes: `set-08/task-2/task.pdf`, `mark.pdf`
- Produces: 이후 태스크의 요구사항 원문. 형식은 `set-08/task-1/task.md`·`mark.md`를 표준으로.

- [ ] **Step 1: pdfminer로 페이지별 추출**

전체 일괄 추출(`extract_text`)은 다단 레이아웃에서 읽기 순서가 섞인다(사전 조사에서 확인). 페이지별 + LAParams 조정으로 추출:

```powershell
py -c @"
from pdfminer.high_level import extract_text
from pdfminer.layout import LAParams
la = LAParams(line_margin=0.3, boxes_flow=None)  # boxes_flow=None: 물리적 위→아래 순서
for src, dst in [('set-08/task-2/task.pdf','task2-pages.txt'), ('set-08/task-2/mark.pdf','mark2-pages.txt')]:
    with open(dst,'w',encoding='utf-8') as f:
        i = 0
        while True:
            t = extract_text(src, page_numbers=[i], laparams=la)
            if not t.strip(): break
            f.write(f'===== PAGE {i+1} =====\n{t}\n'); i += 1
"@
```

- [ ] **Step 2: task.md 작성**

추출 텍스트를 읽고 `set-08/task-1/task.md` 형식(제목·시행 유의사항·과제 개요·모듈별 섹션 3-x~6-x)으로 재구성. 원문 문장 유지, 레이아웃 깨진 부분만 이어붙임. 숫자·이름·환경변수는 PDF와 대조 (특히: pollingInterval 15 이하, cooldownPeriod 30초 이하, PROCESSING_SECONDS 5초, visibility 30초 이상, 3모듈 180초 이내 제거, 유의사항 10번 skills-ceh-protected-sg TCP/22 테스트 조항, 12번 Karpenter/KEDA 버전 조항).

- [ ] **Step 3: mark.md 작성**

`set-08/task-1/mark.md` 형식으로 채점표(모듈별 배점 1-1~4-6)·사전 준비(kubectl 설치 스니펫·export 변수)·항목별 검증 명령 재구성. 말미에 협의회(2026-07-31) 추적 항목 추가:

```markdown
> **[추적] 공식 예상 출력**: 채점지에 예상 출력이 없어 주최측이 별도 텍스트 파일로
> 제공 예정(2026-07-31 지도교사협의회). 도착 시 이 문서·NOTES.md 커버리지와 대조할 것.
```

- [ ] **Step 4: 검증 — 채점 스크립트와 교차 대조**

mark.md의 모듈 2·4 검증 명령·리소스명·숫자를 `mark/mark2-2.sh`·`mark2-4.sh`와 대조 (스크립트가 ground truth). 불일치 시 PDF 재확인 후 수정. task-1 형식과 섹션 구조 일치 확인.

- [ ] **Step 5: Commit**

```powershell
git add set-08/task-2/task.md set-08/task-2/mark.md
git commit -m "Add set-08 task-2 task and mark documents"
```

---

### Task 3: module-2 terraform — 네트워크·SG

**Files:**
- Create: `set-08/task-2/module-2-lattice/terraform/versions.tf`
- Create: `set-08/task-2/module-2-lattice/terraform/variables.tf`
- Create: `set-08/task-2/module-2-lattice/terraform/terraform.tfvars`
- Create: `set-08/task-2/module-2-lattice/terraform/vpc.tf`
- Create: `set-08/task-2/module-2-lattice/terraform/sg.tf`

**Interfaces:**
- Produces: `aws_vpc.client`/`aws_vpc.service`, `aws_subnet.client`/`aws_subnet.service`, `aws_security_group.client`/`aws_security_group.service`/`aws_security_group.sn_assoc`, `data.aws_ec2_managed_prefix_list.vpc_lattice` — Task 4의 ec2.tf·lattice.tf가 참조.

- [ ] **Step 1: versions.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

terraform {
  required_version = ">= 1.6.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.56"
    }
  }
}

provider "aws" {
  region = var.region

  default_tags {
    tags = {
      Project   = "skills-lattice"
      ManagedBy = "terraform"
    }
  }
}
```

- [ ] **Step 2: variables.tf + terraform.tfvars**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# 채점(mark2-2.sh)이 이름·CIDR 정확 일치로 검사하는 값 전부 변수 (30% 변동 대비).

variable "region" {
  description = "VPC Lattice 모듈 리전 (과제지 4)"
  type        = string
  default     = "ap-northeast-1"
}

variable "client_vpc_name" {
  description = "Client VPC Name 태그 (채점 2-1)"
  type        = string
  default     = "skills-lattice-client-vpc"
}

variable "client_vpc_cidr" {
  type    = string
  default = "10.61.0.0/16"
}

variable "client_subnet_cidr" {
  type    = string
  default = "10.61.0.0/24"
}

variable "service_vpc_name" {
  description = "Service VPC Name 태그 (채점 2-1)"
  type        = string
  default     = "skills-lattice-service-vpc"
}

variable "service_vpc_cidr" {
  type    = string
  default = "10.62.0.0/16"
}

variable "service_subnet_cidr" {
  type    = string
  default = "10.62.0.0/24"
}

variable "az" {
  description = "단일 AZ 배치 (과제지 무요구 — 최소 구성)"
  type        = string
  default     = "ap-northeast-1a"
}

variable "client_ec2_name" {
  description = "Client EC2 Name 태그 (채점 2-2)"
  type        = string
  default     = "skills-lattice-client-ec2"
}

variable "service_ec2_name" {
  description = "Service EC2 Name 태그 (채점 2-2)"
  type        = string
  default     = "skills-lattice-service-ec2"
}

variable "instance_type" {
  type    = string
  default = "t3.micro"
}

variable "client_port" {
  description = "Client 앱 Listen 포트 (과제지 4-2)"
  type        = number
  default     = 80
}

variable "service_port" {
  description = "Service 앱 Listen 포트 (과제지 4-3)"
  type        = number
  default     = 8080
}

variable "sn_name" {
  description = "Service Network 이름 (채점 2-3)"
  type        = string
  default     = "skills-lattice-sn"
}

variable "lattice_service_name" {
  description = "Lattice Service 이름 (채점 2-3)"
  type        = string
  default     = "skills-lattice-order-service"
}

variable "tg_name" {
  description = "Target Group 이름 (채점 2-4)"
  type        = string
  default     = "skills-lattice-order-tg"
}

variable "listener_name" {
  description = "Listener 이름 (채점 2-4)"
  type        = string
  default     = "skills-lattice-http-listener"
}
```

terraform.tfvars는 빈 파일 + 주석("기본값 = 과제지 값. 대회 당일 변동분만 여기 기입") — set별 주입 지점 명시용.

- [ ] **Step 3: vpc.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# ---------------------------------------------------------------------------
# VPC 2개 (과제지 4-1). Client ↔ Service 간 peering/TGW 금지 —
# 연결은 VPC Lattice 데이터 플레인만 사용한다 (lattice.tf).
# 두 EC2 모두 Public IP 필요 (채점 2-2가 PublicIp 필드 확인) → public 서브넷.
# ---------------------------------------------------------------------------

resource "aws_vpc" "client" {
  cidr_block           = var.client_vpc_cidr
  enable_dns_support   = true
  enable_dns_hostnames = true
  tags                 = { Name = var.client_vpc_name }
}

resource "aws_vpc" "service" {
  cidr_block           = var.service_vpc_cidr
  enable_dns_support   = true
  enable_dns_hostnames = true
  tags                 = { Name = var.service_vpc_name }
}

resource "aws_internet_gateway" "client" {
  vpc_id = aws_vpc.client.id
  tags   = { Name = "${var.client_vpc_name}-igw" }
}

resource "aws_internet_gateway" "service" {
  vpc_id = aws_vpc.service.id
  tags   = { Name = "${var.service_vpc_name}-igw" }
}

resource "aws_subnet" "client" {
  vpc_id                  = aws_vpc.client.id
  cidr_block              = var.client_subnet_cidr
  availability_zone       = var.az
  map_public_ip_on_launch = true
  tags                    = { Name = "${var.client_vpc_name}-sn" }
}

resource "aws_subnet" "service" {
  vpc_id                  = aws_vpc.service.id
  cidr_block              = var.service_subnet_cidr
  availability_zone       = var.az
  map_public_ip_on_launch = true
  tags                    = { Name = "${var.service_vpc_name}-sn" }
}

resource "aws_route_table" "client" {
  vpc_id = aws_vpc.client.id
  tags   = { Name = "${var.client_vpc_name}-rtb" }
}

resource "aws_route" "client_igw" {
  route_table_id         = aws_route_table.client.id
  destination_cidr_block = "0.0.0.0/0"
  gateway_id             = aws_internet_gateway.client.id
}

resource "aws_route_table_association" "client" {
  subnet_id      = aws_subnet.client.id
  route_table_id = aws_route_table.client.id
}

resource "aws_route_table" "service" {
  vpc_id = aws_vpc.service.id
  tags   = { Name = "${var.service_vpc_name}-rtb" }
}

resource "aws_route" "service_igw" {
  route_table_id         = aws_route_table.service.id
  destination_cidr_block = "0.0.0.0/0"
  gateway_id             = aws_internet_gateway.service.id
}

resource "aws_route_table_association" "service" {
  subnet_id      = aws_subnet.service.id
  route_table_id = aws_route_table.service.id
}
```

- [ ] **Step 4: sg.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# ---------------------------------------------------------------------------
# Security Group 3개 (과제지 4-2·4-3·4-4)
# - client   : in 80 from 0.0.0.0/0 (과제지 명시), out 은 Lattice managed
#              prefix list 80 만 — Lattice 도메인은 link-local 대역으로
#              해석되며 그 대역이 이 prefix list 에 들어있다.
#              DNS 는 VPC 리졸버 경유라 SG 평가 대상이 아니므로 별도 규칙 불필요.
# - service  : in 8080 from Lattice managed prefix list 만.
#              0.0.0.0/0 허용 시 채점 미충족 명시 (과제지 4-3) — Public IP
#              직접 접근 차단은 이 SG 가 담당한다.
# - sn_assoc : SN-VPC association 용. in 80 from Client VPC CIDR (과제지 4-4).
# ---------------------------------------------------------------------------

data "aws_ec2_managed_prefix_list" "vpc_lattice" {
  name = "com.amazonaws.${var.region}.vpc-lattice"
}

resource "aws_security_group" "client" {
  name        = "${var.client_ec2_name}-sg"
  description = "lattice client app"
  vpc_id      = aws_vpc.client.id
  tags        = { Name = "${var.client_ec2_name}-sg" }

  ingress {
    description = "client app from anywhere"
    from_port   = var.client_port
    to_port     = var.client_port
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    description     = "to vpc lattice data plane"
    from_port       = 80
    to_port         = 80
    protocol        = "tcp"
    prefix_list_ids = [data.aws_ec2_managed_prefix_list.vpc_lattice.id]
  }
}

resource "aws_security_group" "service" {
  name        = "${var.service_ec2_name}-sg"
  description = "lattice service app"
  vpc_id      = aws_vpc.service.id
  tags        = { Name = "${var.service_ec2_name}-sg" }

  ingress {
    description     = "service app from vpc lattice only"
    from_port       = var.service_port
    to_port         = var.service_port
    protocol        = "tcp"
    prefix_list_ids = [data.aws_ec2_managed_prefix_list.vpc_lattice.id]
  }
}

resource "aws_security_group" "sn_assoc" {
  name        = "${var.sn_name}-assoc-sg"
  description = "lattice sn vpc association"
  vpc_id      = aws_vpc.client.id
  tags        = { Name = "${var.sn_name}-assoc-sg" }

  ingress {
    description = "http from client vpc"
    from_port   = var.client_port
    to_port     = var.client_port
    protocol    = "tcp"
    cidr_blocks = [var.client_vpc_cidr]
  }
}
```

- [ ] **Step 5: fmt + validate**

```powershell
terraform -chdir=set-08/task-2/module-2-lattice/terraform init -backend=false
terraform -chdir=set-08/task-2/module-2-lattice/terraform fmt -check -recursive
terraform -chdir=set-08/task-2/module-2-lattice/terraform validate
```
Expected: validate 는 ec2.tf·lattice.tf 부재로 아직 통과해야 정상 (참조 없음). 실패 시 이 태스크 파일만으로 원인 해결.

- [ ] **Step 6: Commit**

```powershell
git add set-08/task-2/module-2-lattice
git commit -m "Add module-2 lattice network and security groups"
```

---

### Task 4: module-2 terraform — EC2·Lattice·outputs

**Files:**
- Create: `set-08/task-2/module-2-lattice/terraform/ec2.tf`
- Create: `set-08/task-2/module-2-lattice/terraform/userdata-client.sh.tftpl`
- Create: `set-08/task-2/module-2-lattice/terraform/userdata-service.sh.tftpl`
- Create: `set-08/task-2/module-2-lattice/terraform/lattice.tf`
- Create: `set-08/task-2/module-2-lattice/terraform/outputs.tf`

**Interfaces:**
- Consumes: Task 3의 `aws_vpc.*`, `aws_subnet.*`, `aws_security_group.*`, 변수 전부
- Produces: `aws_instance.client`/`aws_instance.service`, `aws_vpclattice_service.order`(+`dns_entry`), outputs `client_public_ip`/`service_domain`/`service_network_id`/`target_group_id`/`service_id`

- [ ] **Step 1: userdata-service.sh.tftpl**

지급 앱은 stdlib 전용 — pip 불필요. base64 임베드 + systemd(set-07 검증 패턴):

```bash
#!/bin/bash
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# set -e 미사용: 일부 단계 실패해도 systemd 등록까지 진행 (Restart=always 복구 여지)
set -uxo pipefail
mkdir -p /opt/app
base64 -d > /opt/app/service_app.py <<'B64'
${app_b64}
B64
cat > /etc/systemd/system/lattice-service.service <<'UNIT'
[Unit]
Description=lattice service app
After=network.target
[Service]
ExecStart=/usr/bin/python3 /opt/app/service_app.py
Restart=always
[Install]
WantedBy=multi-user.target
UNIT
systemctl daemon-reload
systemctl enable --now lattice-service
```

- [ ] **Step 2: userdata-client.sh.tftpl**

```bash
#!/bin/bash
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
set -uxo pipefail
mkdir -p /opt/app
base64 -d > /opt/app/client_app.py <<'B64'
${app_b64}
B64
cat > /etc/systemd/system/lattice-client.service <<'UNIT'
[Unit]
Description=lattice client app
After=network.target
[Service]
Environment=SERVICE_URL=${service_url}
ExecStart=/usr/bin/python3 /opt/app/client_app.py
Restart=always
[Install]
WantedBy=multi-user.target
UNIT
systemctl daemon-reload
systemctl enable --now lattice-client
```

- [ ] **Step 3: ec2.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# ---------------------------------------------------------------------------
# EC2 2대 (과제지 4-2·4-3). 지급 앱 원본을 base64 로 user-data 임베드
# (provided/ 무수정). client 의 SERVICE_URL 은 Lattice service 의 generated
# domain 을 terraform 참조로 주입 — 의존성 순서 자동 해결.
# 대가: 도메인 변경 시 user_data 변경으로 client 인스턴스 재생성.
# ---------------------------------------------------------------------------

data "aws_ssm_parameter" "al2023_ami" {
  name = "/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64"
}

resource "aws_instance" "service" {
  ami                    = data.aws_ssm_parameter.al2023_ami.value
  instance_type          = var.instance_type
  subnet_id              = aws_subnet.service.id
  vpc_security_group_ids = [aws_security_group.service.id]

  user_data = templatefile("${path.module}/userdata-service.sh.tftpl", {
    app_b64 = base64encode(file("${path.module}/../../provided/module-2/service_app.py"))
  })

  tags = { Name = var.service_ec2_name }
}

resource "aws_instance" "client" {
  ami                    = data.aws_ssm_parameter.al2023_ami.value
  instance_type          = var.instance_type
  subnet_id              = aws_subnet.client.id
  vpc_security_group_ids = [aws_security_group.client.id]

  user_data = templatefile("${path.module}/userdata-client.sh.tftpl", {
    app_b64     = base64encode(file("${path.module}/../../provided/module-2/client_app.py"))
    service_url = "http://${aws_vpclattice_service.order.dns_entry[0].domain_name}"
  })

  tags = { Name = var.client_ec2_name }
}
```

- [ ] **Step 4: lattice.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# ---------------------------------------------------------------------------
# VPC Lattice (과제지 4-4). 채점 2-3·2-4 가 name 필드 정확 일치로 조회하므로
# 리소스 name = 과제지 값. auth 는 과제지 무요구 → NONE.
# SN-VPC association 은 Client VPC 만 (소비 측만 연결 필요 — target 은
# TG 의 vpc_identifier 로 직접 도달).
# ---------------------------------------------------------------------------

resource "aws_vpclattice_service_network" "this" {
  name      = var.sn_name
  auth_type = "NONE"
  tags      = { Name = var.sn_name }
}

resource "aws_vpclattice_service_network_vpc_association" "client" {
  vpc_identifier             = aws_vpc.client.id
  service_network_identifier = aws_vpclattice_service_network.this.id
  security_group_ids         = [aws_security_group.sn_assoc.id]
}

resource "aws_vpclattice_service" "order" {
  name      = var.lattice_service_name
  auth_type = "NONE"
  tags      = { Name = var.lattice_service_name }
}

resource "aws_vpclattice_service_network_service_association" "order" {
  service_identifier         = aws_vpclattice_service.order.id
  service_network_identifier = aws_vpclattice_service_network.this.id
}

resource "aws_vpclattice_target_group" "order" {
  name = var.tg_name
  type = "INSTANCE"
  tags = { Name = var.tg_name }

  config {
    vpc_identifier = aws_vpc.service.id
    port           = var.service_port
    protocol       = "HTTP"

    health_check {
      enabled  = true
      path     = "/health"
      protocol = "HTTP"
      port     = var.service_port
    }
  }
}

resource "aws_vpclattice_target_group_attachment" "service" {
  target_group_identifier = aws_vpclattice_target_group.order.id

  target {
    id   = aws_instance.service.id
    port = var.service_port
  }
}

resource "aws_vpclattice_listener" "http" {
  name               = var.listener_name
  protocol           = "HTTP"
  port               = var.client_port
  service_identifier = aws_vpclattice_service.order.id

  default_action {
    forward {
      target_groups {
        target_group_identifier = aws_vpclattice_target_group.order.id
        weight                  = 100
      }
    }
  }
}
```

- [ ] **Step 5: outputs.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# mark2-2.sh 가 조회하는 값과 동일 형태 — 배포 후 즉시 자가 검증용.

output "client_public_ip" {
  value = aws_instance.client.public_ip
}

output "service_domain" {
  value = aws_vpclattice_service.order.dns_entry[0].domain_name
}

output "service_network_id" {
  value = aws_vpclattice_service_network.this.id
}

output "service_id" {
  value = aws_vpclattice_service.order.id
}

output "target_group_id" {
  value = aws_vpclattice_target_group.order.id
}
```

- [ ] **Step 6: fmt + validate + plan**

```powershell
terraform -chdir=set-08/task-2/module-2-lattice/terraform fmt -check -recursive
terraform -chdir=set-08/task-2/module-2-lattice/terraform validate
terraform -chdir=set-08/task-2/module-2-lattice/terraform plan
```
Expected: validate 통과. plan 은 자격증명 있으면 리소스 생성 목록(에러 0) — `aws_vpclattice_*` 인자 오류가 여기서 잡힌다. 자격증명 없으면 "credentials 없음 — plan 미실행" 기록.

- [ ] **Step 7: Commit**

```powershell
git add set-08/task-2/module-2-lattice
git commit -m "Add module-2 lattice EC2 apps and VPC Lattice resources"
```

---

### Task 5: module-2 런북

**Files:**
- Create: `set-08/task-2/module-2-lattice/README.md`
- Create: `set-08/task-2/module-2-lattice/README.linux.md`

**Interfaces:**
- Consumes: Task 4 outputs (`client_public_ip`, `service_domain`)

- [ ] **Step 1: README.md (PS7 런북)**

명령형 단계만. 구성:

```markdown
# module-2 lattice 런북

## 0. IAM 권한 프로브 (대회 시작 직후 1회)
(이 모듈은 IAM 생성 불필요 — 프로브는 module-4 런북 0단계에서 수행)

## 1. 배포
terraform -chdir=terraform init
terraform -chdir=terraform apply

## 2. 환경변수 (.env)
terraform -chdir=terraform output 값으로 로컬 .env 작성 (재접속 대비, 작업 규칙 6):
  CLIENT_IP / SERVICE_DOMAIN / SERVICE_NETWORK_ID / SERVICE_ID / TARGET_GROUP_ID

## 3. 검증 (배포 후 ~2분 대기: user-data systemd 기동)
curl http://$CLIENT_IP/health                      → 200 {"status":"ok","app":"client"}
curl "http://$CLIENT_IP/v1/client/orders?id=1001"  → service.order_id=1001, service.via=vpc-lattice
mark/mark2-2.sh 를 CloudShell 에서 실행해 전 항목 확인

## 4. Teardown
terraform -chdir=terraform destroy
```

각 단계는 실제 PS7 문법으로 완성해 작성 (위는 골격 — 작성 시 `$env:` 변수, `Set-Content` 사용. "왜"는 쓰지 않는다).

- [ ] **Step 2: README.linux.md**

동일 단계의 bash/zsh 겸용판. set-07 규칙 준수: CloudShell 단계도 번호 자리에 stub 섹션 유지, `"${VAR}:latest"` 중괄호 규칙(이 모듈은 해당 없음이나 export 스니펫은 zsh 안전하게).

- [ ] **Step 3: 런북 순서 검증**

README 단계 순서가 실제 의존성(apply → 대기 → curl)과 일치하는지, mark2-2.sh 실행 위치(CloudShell)가 명시됐는지 확인.

- [ ] **Step 4: Commit**

```powershell
git add set-08/task-2/module-2-lattice
git commit -m "Add module-2 lattice runbooks"
```

---

### Task 6: module-4 terraform — VPC·SQS·ECR·IAM

**Files:**
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/versions.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/variables.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/terraform.tfvars`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/data.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/vpc.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/sqs.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/ecr.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/iam.tf`
- Create: `set-08/task-2/module-4-sqs-scaling/terraform/outputs.tf`

**Interfaces:**
- Produces: outputs `vpc_id`/`private_subnet_ids`/`queue_url`/`queue_arn`/`ecr_repo_url`/`keda_policy_arn`/`worker_policy_arn`/`karpenter_policy_arn`/`karpenter_node_role_name`/`karpenter_node_role_arn`/`account_id` — Task 7 cluster.yaml·Task 8 manifest 치환이 소비.

- [ ] **Step 1: versions.tf + data.tf**

versions.tf 는 Task 3 Step 1 과 동일 구조 (Project 태그만 `skills-sqs`, region 기본 us-west-2). data.tf:

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

data "aws_caller_identity" "current" {}

locals {
  account_id = data.aws_caller_identity.current.account_id
}
```

- [ ] **Step 2: variables.tf + terraform.tfvars**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# 채점(mark2-4.sh)이 이름 정확 일치로 검사하는 값 전부 변수 (30% 변동 대비).

variable "region" {
  description = "SQS Scaling 모듈 리전 (과제지 6)"
  type        = string
  default     = "us-west-2"
}

variable "cluster_name" {
  description = "EKS 클러스터 이름 (채점 4-1)"
  type        = string
  default     = "skills-sqs-cluster"
}

variable "queue_name" {
  description = "SQS 큐 이름 (채점 4-2)"
  type        = string
  default     = "skills-sqs-queue"
}

variable "visibility_timeout" {
  description = "과제지 6-3: 30초 이상"
  type        = number
  default     = 30
}

variable "ecr_repo_name" {
  description = "worker 이미지 저장소 (미채점 — 이미지 출처 자유)"
  type        = string
  default     = "skills-sqs-worker"
}

variable "name_prefix" {
  description = "미채점 리소스 Name 태그 접두사"
  type        = string
  default     = "skills-sqs"
}

variable "vpc_cidr" {
  type    = string
  default = "10.64.0.0/16"
}

variable "subnets" {
  description = "서브넷 정의 (Fargate/Karpenter 노드는 private)"
  type = map(object({
    cidr = string
    az   = string
    tier = string # public | private
  }))
  default = {
    "skills-sqs-sn-pub-a"  = { cidr = "10.64.0.0/24", az = "us-west-2a", tier = "public" }
    "skills-sqs-sn-pub-b"  = { cidr = "10.64.1.0/24", az = "us-west-2b", tier = "public" }
    "skills-sqs-sn-priv-a" = { cidr = "10.64.10.0/24", az = "us-west-2a", tier = "private" }
    "skills-sqs-sn-priv-b" = { cidr = "10.64.11.0/24", az = "us-west-2b", tier = "private" }
  }
}
```

- [ ] **Step 3: vpc.tf**

set-07 `module-3-eks-scaling/terraform/vpc.tf` 를 복사해 다음만 수정 (구조 검증된 파일 재사용):

```powershell
Copy-Item set-07/task-2/module-3-eks-scaling/terraform/vpc.tf set-08/task-2/module-4-sqs-scaling/terraform/vpc.tf
```
- 헤더 주석의 과제지 참조를 "과제지 6-1" 로 변경
- `locals { public_subnet_keys / private_subnet_keys }` 정의가 set-07 에서는 별도 파일에 있으면 이 파일 상단으로 가져온다:

```hcl
locals {
  public_subnet_keys  = [for k, v in var.subnets : k if v.tier == "public"]
  private_subnet_keys = [for k, v in var.subnets : k if v.tier == "private"]
}
```
- NAT 는 AZ 별 → 비용 절감 위해 그대로 유지 (채점 무관, 검증된 구조 우선)
- 나머지(karpenter.sh/discovery 태그 포함)는 변경 없음 — 변수 이름이 동일해 그대로 동작

- [ ] **Step 4: sqs.tf + ecr.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# SQS Standard 큐 (과제지 6-3, 채점 4-2 가 QueueArn·VisibilityTimeout 확인)
resource "aws_sqs_queue" "worker" {
  name                       = var.queue_name
  visibility_timeout_seconds = var.visibility_timeout
}
```

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# worker 컨테이너 이미지 저장소 (과제지 6-5 — 이미지 직접 작성.
# 빌드·푸시는 CloudShell 에서 수행, README 3단계)
resource "aws_ecr_repository" "worker" {
  name         = var.ecr_repo_name
  force_delete = true
}
```

- [ ] **Step 5: iam.tf**

set-07 iam.tf 를 복사해 수정 (Karpenter 컨트롤러 정책 JSON 은 검증본 재사용):

```powershell
Copy-Item set-07/task-2/module-3-eks-scaling/terraform/iam.tf set-08/task-2/module-4-sqs-scaling/terraform/iam.tf
```
수정 목록 (전부 기계적 치환):
- `skm-keda-policy` → `"${var.name_prefix}-keda-policy"` / `skm-app-sqs-policy` → `"${var.name_prefix}-worker-policy"` / `skm-karpenter-policy` → `"${var.name_prefix}-karpenter-policy"`
- `aws_sqs_queue.order` → `aws_sqs_queue.worker` (2곳)
- worker 정책 Action 에 `"sqs:GetQueueUrl"` 추가 (worker.py 는 QueueUrl 을 env 로 받아 불필요하지만 KEDA fallback·수동 검증용 — 채점 무관 최소 추가)
- 헤더 주석의 채점 항목 참조를 4-2·4-5 로 변경
- KarpenterNodeRole 이름·정책 JSON 의 `var.cluster_name`/`var.region`/`local.account_id` 참조는 변수명 동일 — 변경 없음
- `AmazonEC2ContainerRegistryReadOnly` 등 4개 관리형 정책 attachment 변경 없음

- [ ] **Step 6: outputs.tf**

```hcl
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors

# cluster.yaml·k8s manifest 치환(README 2·5단계)과 .env 작성이 소비한다.

output "vpc_id" {
  value = aws_vpc.this.id
}

output "private_subnet_ids" {
  value = { for k in local.private_subnet_keys : k => aws_subnet.this[k].id }
}

output "queue_url" {
  value = aws_sqs_queue.worker.url
}

output "queue_arn" {
  value = aws_sqs_queue.worker.arn
}

output "ecr_repo_url" {
  value = aws_ecr_repository.worker.repository_url
}

output "keda_policy_arn" {
  value = aws_iam_policy.keda.arn
}

output "worker_policy_arn" {
  value = aws_iam_policy.app_sqs.arn
}

output "karpenter_policy_arn" {
  value = aws_iam_policy.karpenter.arn
}

output "karpenter_node_role_name" {
  value = aws_iam_role.karpenter_node.name
}

output "karpenter_node_role_arn" {
  value = aws_iam_role.karpenter_node.arn
}

output "account_id" {
  value = local.account_id
}
```
(리소스 참조명 `aws_iam_policy.app_sqs` 는 set-07 복사본의 이름 그대로 — outputs 이 실제 파일과 일치하는지 validate 로 확인)

- [ ] **Step 7: fmt + validate + plan**

```powershell
terraform -chdir=set-08/task-2/module-4-sqs-scaling/terraform init -backend=false
terraform -chdir=set-08/task-2/module-4-sqs-scaling/terraform fmt -check -recursive
terraform -chdir=set-08/task-2/module-4-sqs-scaling/terraform validate
terraform -chdir=set-08/task-2/module-4-sqs-scaling/terraform plan
```
Expected: validate 통과. plan 자격증명 없으면 기록 후 진행.

- [ ] **Step 8: Commit**

```powershell
git add set-08/task-2/module-4-sqs-scaling
git commit -m "Add module-4 terraform infra (VPC, SQS, ECR, IAM)"
```

---

### Task 7: module-4 eksctl cluster.yaml + Dockerfile

**Files:**
- Create: `set-08/task-2/module-4-sqs-scaling/eksctl/cluster.yaml`
- Create: `set-08/task-2/module-4-sqs-scaling/app/Dockerfile`

**Interfaces:**
- Consumes: Task 6 outputs (`${ACCOUNT_ID}`/`${VPC_ID}`/`${PRIV_SUBNET_A}`/`${PRIV_SUBNET_B}` placeholder 로)
- Produces: Fargate 클러스터 + SA 3개 (keda/keda-operator, karpenter/karpenter, skills-sqs/sqs-worker-sa — helm·manifest 가 이름으로 재사용)

- [ ] **Step 1: cluster.yaml**

```yaml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# ===========================================================================
# eksctl Cluster Manifest — SQS Scaling (과제지 6-1·6-2·6-4)
#
# ${ACCOUNT_ID} ${VPC_ID} ${PRIV_SUBNET_A} ${PRIV_SUBNET_B} 는 terraform output
# 으로 치환 후 사용 (README 2단계 — 치환 가드 2단계 필수).
#   eksctl create cluster -f rendered/cluster.yaml
#
# - 노드그룹 없음: 컨트롤러(KEDA·Karpenter)는 Fargate, 워커는 Karpenter EC2.
# - Fargate profile 은 과제지 명시 2개(keda·karpenter) + kube-system 1개 추가:
#   노드그룹이 없어 CoreDNS 가 앉을 곳이 없다 — coredns addon 을
#   computeType=Fargate 로 강제하고 kube-system profile 이 이를 받는다.
#   (채점 4-1 은 명시 2개 profile 존재만 검사)
# - CloudShell kubectl 요구(협의회 확정) → public 엔드포인트 +
#   API_AND_CONFIG_MAP + access entry.
# ===========================================================================
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: skills-sqs-cluster
  region: us-west-2
  tags:
    karpenter.sh/discovery: skills-sqs-cluster

# Auto Mode 명시적 비활성화 (set-07 결정 계승: 미지정 시 최신 eksctl 이
# Auto Mode 를 켤 수 있다 — 본 과제는 Fargate + Karpenter 설계)
autoModeConfig:
  enabled: false

accessConfig:
  authenticationMode: API_AND_CONFIG_MAP
  accessEntries:
    # Karpenter 노드는 Terraform 생성 역할로 부팅 — eksctl 이 자동 등록하지
    # 않으므로 명시 (누락 시 노드 조인 실패 → 채점 4-6 실패)
    - principalARN: "arn:aws:iam::${ACCOUNT_ID}:role/KarpenterNodeRole-skills-sqs-cluster"
      type: EC2_LINUX

vpc:
  id: "${VPC_ID}"
  subnets:
    private:
      skills-sqs-sn-priv-a:
        id: "${PRIV_SUBNET_A}"
        az: us-west-2a
      skills-sqs-sn-priv-b:
        id: "${PRIV_SUBNET_B}"
        az: us-west-2b
  clusterEndpoints:
    publicAccess: true
    privateAccess: true

# ----- Fargate Profiles (과제지 6-2 + CoreDNS 용 추가 1개) -----
fargateProfiles:
  - name: skills-sqs-fp-keda
    selectors:
      - namespace: keda
  - name: skills-sqs-fp-karpenter
    selectors:
      - namespace: karpenter
  - name: skills-sqs-fp-kube-system
    selectors:
      - namespace: kube-system

# ----- OIDC + IRSA (과제지 6-4: SA 3개에 role-arn annotation) -----
# helm 설치 시 serviceAccount.create=false 로 재사용 (README 4단계).
iam:
  withOIDC: true
  serviceAccounts:
    - metadata:
        name: keda-operator
        namespace: keda
      attachPolicyARNs:
        - "arn:aws:iam::${ACCOUNT_ID}:policy/skills-sqs-keda-policy"
    - metadata:
        name: karpenter
        namespace: karpenter
      attachPolicyARNs:
        - "arn:aws:iam::${ACCOUNT_ID}:policy/skills-sqs-karpenter-policy"
    - metadata:
        name: sqs-worker-sa
        namespace: skills-sqs
      attachPolicyARNs:
        - "arn:aws:iam::${ACCOUNT_ID}:policy/skills-sqs-worker-policy"

# ----- Addons (버전 미핀 — 작업 규칙 2: 최신 안정) -----
# coredns 는 Fargate 강제: 노드그룹이 없으므로 computeType 미지정 시 Pending.
addons:
  - name: vpc-cni
  - name: kube-proxy
  - name: coredns
    configurationValues: |
      {"computeType": "Fargate"}
```

- [ ] **Step 2: app/Dockerfile**

```dockerfile
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# 지급 worker.py 무수정 사용 (과제지 6-5: boto3 포함, Dockerfile 직접 작성).
# 빌드 컨텍스트에 provided/module-4/worker.py 를 복사해 두고 빌드한다
# (README 3단계 CloudShell — cp ../provided/module-4/worker.py .)
FROM public.ecr.aws/docker/library/python:3.12-slim
RUN pip install --no-cache-dir boto3
COPY worker.py /app/worker.py
CMD ["python", "-u", "/app/worker.py"]
```

- [ ] **Step 3: 스키마 검증**

```powershell
py -c "import yaml,sys; yaml.safe_load(open('set-08/task-2/module-4-sqs-scaling/eksctl/cluster.yaml',encoding='utf-8')); print('yaml ok')"
```
Expected: `yaml ok` (pyyaml 없으면 `py -m pip install pyyaml`). placeholder 4종(`${ACCOUNT_ID}`/`${VPC_ID}`/`${PRIV_SUBNET_A}`/`${PRIV_SUBNET_B}`)이 전부 존재하는지 grep 확인.

- [ ] **Step 4: Commit**

```powershell
git add set-08/task-2/module-4-sqs-scaling
git commit -m "Add module-4 eksctl cluster manifest and worker Dockerfile"
```

---

### Task 8: module-4 k8s manifests

**Files:**
- Create: `set-08/task-2/module-4-sqs-scaling/k8s/00-namespace.yaml`
- Create: `set-08/task-2/module-4-sqs-scaling/k8s/10-karpenter-nodepool.yaml`
- Create: `set-08/task-2/module-4-sqs-scaling/k8s/20-deployment.yaml`
- Create: `set-08/task-2/module-4-sqs-scaling/k8s/30-keda-scaledobject.yaml`

**Interfaces:**
- Consumes: placeholder `${QUEUE_URL}` `${ECR_IMAGE}` `${REGION}` (terraform output, README 5단계 치환), SA `sqs-worker-sa`(Task 7 eksctl 생성), helm 설치된 KEDA·Karpenter CRD
- Produces: 채점 4-4·4-5 가 이름으로 조회하는 리소스 전부

- [ ] **Step 1: 00-namespace.yaml**

```yaml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# eksctl IRSA(sqs-worker-sa) 생성 시 이미 만들어질 수 있으나 apply 멱등이므로 유지.
apiVersion: v1
kind: Namespace
metadata:
  name: skills-sqs
```

- [ ] **Step 2: 10-karpenter-nodepool.yaml**

```yaml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# ---------------------------------------------------------------------------
# Karpenter EC2NodeClass / NodePool (과제지 6-7)
# - NodePool label skills-nodepool=event-worker (채점 4-5 노드 셀렉터와 일치)
# - consolidationPolicy: 과제지는 "설정 포함"만 요구 — WhenEmpty + 30s 채택:
#   min 0 스케일이라 큐 소진 후 빈 노드 즉시 반환이 목적에 부합.
# subnet: karpenter.sh/discovery 태그(terraform private 서브넷) 디스커버리
# securityGroup: 클러스터 기본 SG (aws:eks:cluster-name 태그) 디스커버리
# role: terraform 이 만든 KarpenterNodeRole-skills-sqs-cluster
# ---------------------------------------------------------------------------
apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: skills-sqs-nodeclass
spec:
  amiFamily: AL2023
  amiSelectorTerms:
    - alias: al2023@latest
  role: "KarpenterNodeRole-skills-sqs-cluster"
  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: "skills-sqs-cluster"
  securityGroupSelectorTerms:
    - tags:
        aws:eks:cluster-name: "skills-sqs-cluster"
---
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: skills-sqs-nodepool
spec:
  template:
    metadata:
      labels:
        skills-nodepool: event-worker
    spec:
      nodeClassRef:
        group: karpenter.k8s.aws
        kind: EC2NodeClass
        name: skills-sqs-nodeclass
      requirements:
        - key: kubernetes.io/arch
          operator: In
          values: ["amd64"]
        - key: kubernetes.io/os
          operator: In
          values: ["linux"]
        - key: karpenter.sh/capacity-type
          operator: In
          values: ["on-demand"]
        - key: node.kubernetes.io/instance-type
          operator: In
          values: ["t3.medium", "t3.large"]
      expireAfter: 720h
  limits:
    cpu: "16"
  disruption:
    consolidationPolicy: WhenEmpty
    consolidateAfter: 30s
```

- [ ] **Step 3: 20-deployment.yaml**

```yaml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# ---------------------------------------------------------------------------
# Worker Deployment (과제지 6-5)
# - replicas 미지정: KEDA(HPA)가 0~6 을 관리 — 명시하면 재apply 때 스케일 리셋.
# - env 는 과제지 명시 3개만 (채점 4-4 가 env 전체를 덤프 — 여분 추가 금지).
# - nodeSelector 2개: Fargate 로 못 가고 Karpenter 노드에만 배치 (skills-sqs
#   namespace 는 Fargate profile 이 없어 구조적으로도 Fargate 배제).
# - resources.requests: t3.medium(2vCPU) 기준 6 pod = 노드 2대 유도.
# ${ECR_IMAGE} ${QUEUE_URL} ${REGION} 은 README 5단계에서 치환.
# ---------------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sqs-worker
  namespace: skills-sqs
  labels:
    app: sqs-worker
spec:
  selector:
    matchLabels:
      app: sqs-worker
  template:
    metadata:
      labels:
        app: sqs-worker
    spec:
      serviceAccountName: sqs-worker-sa
      nodeSelector:
        karpenter.sh/nodepool: skills-sqs-nodepool
        skills-nodepool: event-worker
      containers:
        - name: worker
          image: "${ECR_IMAGE}"
          env:
            - name: SQS_QUEUE_URL
              value: "${QUEUE_URL}"
            - name: AWS_REGION
              value: "${REGION}"
            - name: PROCESSING_SECONDS
              value: "5"
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
```

- [ ] **Step 4: 30-keda-scaledobject.yaml**

```yaml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The ISHS Cloud Computing Authors
# ---------------------------------------------------------------------------
# KEDA ScaledObject + TriggerAuthentication (과제지 6-6, 채점 4-4 직접 조회)
# - min 0 / max 6, queueLength 2 → 12건 발송 시 6 pod (채점 4-6).
# - pollingInterval 15: min=0 이라 0→1 활성화는 KEDA operator 직접 폴링 —
#   과제지 상한(15초 이하) 그대로. (set-07 은 min=1 이라 삭제했던 필드)
# - podIdentity aws: keda-operator SA 의 IRSA 역할로 SQS 조회
#   (identityOwner 방식은 KEDA 3.0 제거 예정 — TriggerAuthentication 전환)
# ${QUEUE_URL} ${REGION} 은 README 5단계에서 치환.
# ---------------------------------------------------------------------------
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: sqs-worker-trigger-auth
  namespace: skills-sqs
spec:
  podIdentity:
    provider: aws
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: sqs-worker-scaledobject
  namespace: skills-sqs
spec:
  scaleTargetRef:
    name: sqs-worker
  minReplicaCount: 0
  maxReplicaCount: 6
  pollingInterval: 15
  cooldownPeriod: 30
  triggers:
    - type: aws-sqs-queue
      authenticationRef:
        name: sqs-worker-trigger-auth
      metadata:
        queueURL: "${QUEUE_URL}"
        queueLength: "2"
        awsRegion: "${REGION}"
```

- [ ] **Step 5: 검증 — yaml 파싱 + placeholder 목록**

```powershell
Get-ChildItem set-08/task-2/module-4-sqs-scaling/k8s/*.yaml | ForEach-Object {
  py -c "import yaml,sys; list(yaml.safe_load_all(open(r'$($_.FullName)',encoding='utf-8'))); print('ok', r'$($_.Name)')"
}
Select-String -Path set-08/task-2/module-4-sqs-scaling/k8s/*.yaml -Pattern '\$\{' | Select-Object Filename, Line
```
Expected: 전 파일 ok. placeholder 는 정확히 `${ECR_IMAGE}` `${QUEUE_URL}` `${REGION}` 3종만 (README 치환 목록과 일치해야 함).

- [ ] **Step 6: Commit**

```powershell
git add set-08/task-2/module-4-sqs-scaling/k8s
git commit -m "Add module-4 Karpenter and KEDA manifests"
```

---

### Task 9: module-4 런북

**Files:**
- Create: `set-08/task-2/module-4-sqs-scaling/README.md`
- Create: `set-08/task-2/module-4-sqs-scaling/README.linux.md`
- Modify: `.gitignore` (`rendered/`·`.env`·kubeconfig 미포함 시 추가)

**Interfaces:**
- Consumes: Task 6 outputs, Task 7 cluster.yaml, Task 8 manifests

- [ ] **Step 1: README.md (PS7 런북) 작성**

단계 구성 (각 단계 실제 PS7 명령으로 완성):

```markdown
## 0. IAM 권한 프로브 (대회 시작 직후 1회 — 협의회 "PowerUser 이상" 대비)
aws iam create-role --role-name skills-iam-probe --assume-role-policy-document <ec2 assume json>
→ 성공: 정상 (aws iam delete-role 로 정리 — 삭제 거부되면 그대로 두고 감독관에 확인)
→ AccessDenied: IAM 미지급 — 즉시 감독관 문의 (module-1·3·4 진행 불가)

## 1. terraform apply
terraform -chdir=terraform init && apply. output -json 을 outputs.json 으로 저장(커밋 금지).
주요 output 을 로컬 .env 로도 저장 (QUEUE_URL/ECR_IMAGE/REGION/ACCOUNT_ID — 재접속 대비, 작업 규칙 6).

## 2. cluster.yaml 렌더링 + eksctl create (~20분)
치환 가드 2단계 (set-07 규칙):
  ① 치환 전: ACCOUNT_ID/VPC_ID/PRIV_SUBNET_A/PRIV_SUBNET_B 비어있으면 중단
  ② 치환 후: Select-String '\$\{' rendered/cluster.yaml → 잔존 placeholder 있으면 중단
eksctl create cluster -f rendered/cluster.yaml
클러스터 전용 kubeconfig: $env:KUBECONFIG = "$PWD\kubeconfig" (set-07 결정: 터미널 1개 = 클러스터 1개)

## 3. [CloudShell] worker 이미지 build/push
CloudShell 에 Dockerfile + provided/module-4/worker.py 업로드 후:
  aws ecr get-login-password | docker login …
  cp worker.py . ; docker build -t "${ECR_IMAGE}" . ; docker push "${ECR_IMAGE}"
(로컬 Docker 불가 — CloudShell 의 docker 사용. "${VAR}:latest" 중괄호 필수)

## 4. helm — Karpenter·KEDA (버전 미핀: 최신 안정)
helm install karpenter oci://public.ecr.aws/karpenter/karpenter -n karpenter \
  --set settings.clusterName=skills-sqs-cluster \
  --set serviceAccount.create=false --set serviceAccount.name=karpenter \
  --set replicas=1 --set dnsPolicy=Default --wait
  # dnsPolicy=Default: Fargate 기동 시 CoreDNS 의존 제거 (VPC 리졸버 직행)
helm repo add kedacore https://kedacore.github.io/charts
helm install keda kedacore/keda -n keda \
  --set serviceAccount.operator.create=false \
  --set serviceAccount.operator.name=keda-operator --wait
확인: kubectl get pods -n karpenter / -n keda / -n kube-system (coredns) 전부 Running,
kubectl get nodes -l eks.amazonaws.com/compute-type=fargate

## 5. k8s manifest 렌더링 + apply
치환 (ECR_IMAGE = "<ecr_repo_url>:latest", QUEUE_URL, REGION) → rendered/ → 가드 ②
kubectl apply -f rendered/

## 6. 스케일 검증 (mark2-4.sh 4-6 시나리오 수동 재현)
aws sqs send-message ×12 → 60/120/180초 시점 pods·nodes 확인 → 큐 소진 후
cooldown 30s + WhenEmpty 30s 로 pod 0·노드 0 복귀 확인

## 7. [CloudShell] kubectl 확인 (협의회 필수 요구)
aws eks update-kubeconfig --region us-west-2 --name skills-sqs-cluster
kubectl get nodes — 실패 시 access entry 추가 (fallback 명령 명시)

## 8. 채점 전 상시 상태
큐 비움(purge 후 2분 대기), pod 0·Karpenter 노드 0 이 정상 (min 0).

## 9. Teardown
kubectl delete -f rendered/ → helm uninstall ×2 → eksctl delete cluster → terraform destroy
```

- [ ] **Step 2: README.linux.md 작성**

동일 단계 bash/zsh 겸용판. CloudShell 단계(3·7)는 번호 자리 유지(set-07 규칙). `"${VAR}:latest"` 중괄호, `${!v}` 간접 확장 금지, envsubst 사용 시에도 가드 ①(사전 비어있음 검사) 필수.

- [ ] **Step 3: .gitignore 확인**

```powershell
Select-String -Path .gitignore -Pattern "rendered|outputs.json|kubeconfig|\.env"
```
누락 항목 추가: `rendered/`, `**/kubeconfig`, `.env` (outputs.json·tfstate 는 기존 확인).

- [ ] **Step 4: 런북 순서 검증**

의존성 순서(terraform → eksctl → 이미지 → helm → manifest → 검증) 일치, 치환 목록이 Task 7·8 placeholder 와 1:1 인지 대조.

- [ ] **Step 5: Commit**

```powershell
git add set-08/task-2/module-4-sqs-scaling .gitignore
git commit -m "Add module-4 runbooks and ignore rendered artifacts"
```

---

### Task 10: NOTES.md·README.md·docs 사이트

**Files:**
- Create: `set-08/task-2/NOTES.md` (template 덮어쓰기)
- Create: `set-08/task-2/README.md` (template 덮어쓰기)
- Create: `docs/src/content/docs/setlist/set-08/task-2/{overview,mapping,deployment,runbook}.md`
- Modify: `docs/astro.config.mjs` (set-08/task-2 사이드바 항목 — set-07 항목 형식 참조)

**Interfaces:**
- Consumes: 전 태스크 산출물

- [ ] **Step 1: NOTES.md**

set-07/task-2/NOTES.md 형식. 필수 내용:
- 모듈 현황 표: 1·3 미착수 / 2·4 "plan 수준 검증" (`[~]`)
- module-2 채점 커버리지: 2-1~2-5 각 `[~]` + 한 줄 근거 (구현 리소스 명시)
- module-4 채점 커버리지: 4-1~4-6 각 `[~]` + 한 줄 근거
- 함정 절: ① service-sg 0.0.0.0/0 → 명시적 미충족 (과제지 4-3) ② min 0 이라 채점 4-5 시점에 pod·노드 0개일 수 있음 — 4-6 부하 후 재확인 경로 관찰 필요 ③ 삭제 금지 정책 대비: 이름 충돌 시 삭제 대신 변수 리네임 (set-07 의 log group 선삭제 해법은 대회에서 불가 가능) ④ CloudShell 업로드 파일 목록 (Dockerfile·worker.py·mark 스크립트)
- 결정 로그 (맥락/채택/기각/대가 형식, [module-N] 태그):
  - [module-4] fp-kube-system 추가 + coredns computeType=Fargate (기각: 소형 MNG)
  - [module-4] TriggerAuthentication podIdentity aws (기각: identityOwner — 3.0 제거 예정)
  - [module-4] pollingInterval 15 유지 — min 0 이라 유효 (set-07 삭제 결정의 전제가 뒤집힘)
  - [module-4] consolidationPolicy WhenEmpty/30s (기각: WhenEmptyOrUnderutilized — 과제지가 값 미지정, 빈 노드 반환만 필요)
  - [module-4] Karpenter dnsPolicy=Default (기각: 기본 ClusterFirst — CoreDNS 기동 의존 순환 리스크)
  - [module-2] SERVICE_URL 을 terraform 참조로 주입 (기각: 수동 치환 — 순서 의존 추가)
  - [module-2] service-sg prefix list 소스 (기각: 0.0.0.0/0 — 명시적 미충족)
  - [공통] IAM 생성 권한 지급 전제 + 런북 0단계 프로브 (협의회 "PowerUser 이상")
- 협의회 추적: 공식 예상 출력 파일 도착 시 mark.md·커버리지 대조

- [ ] **Step 2: task-2 README.md**

모듈별 런북 링크 + 공통 사항(리전 4개 주의, .env 규칙, mark/ 실행 위치는 CloudShell)만. 절차 중복 금지 (각 모듈 README 가 소유).

- [ ] **Step 3: docs 사이트 4페이지**

set-07/task-2 대응 문서 형식 참조 (Explanation — 왜/트레이드오프만):
- `overview.md`: 4모듈 구성과 리전 분산, 이번 구현 범위(2·4)
- `mapping.md`: 채점 항목 ↔ 리소스 표 (모듈 2·4)
- `deployment.md`: 도구 3분담(terraform/eksctl/helm) 이유, Fargate 컨트롤러 선택 이유
- `runbook.md`: 런북 설계 노트 (치환 가드, CloudShell 단계 배치 이유)
astro.config.mjs 사이드바에 set-08/task-2 추가 (set-07 항목과 같은 형식).

- [ ] **Step 4: docs 빌드 검증**

```powershell
cd docs; npm run build
```
Expected: 빌드 성공, set-08/task-2 4페이지 생성 확인. (npm 미설치 환경이면 결과에 기록하고 진행)

- [ ] **Step 5: Commit**

```powershell
git add set-08/task-2/NOTES.md set-08/task-2/README.md docs
git commit -m "Add set-08 task-2 notes, readme, and docs pages"
```

---

### Task 11: 최종 점검

**Files:**
- Modify: 발견된 문제 파일

- [ ] **Step 1: 전체 fmt/validate/plan 재실행**

```powershell
terraform -chdir=set-08/task-2/module-2-lattice/terraform fmt -check -recursive
terraform -chdir=set-08/task-2/module-2-lattice/terraform validate
terraform -chdir=set-08/task-2/module-4-sqs-scaling/terraform fmt -check -recursive
terraform -chdir=set-08/task-2/module-4-sqs-scaling/terraform validate
```

- [ ] **Step 2: 채점 대조 최종 확인**

mark2-2.sh·mark2-4.sh 의 모든 조회 명령을 훑고, 각 조회가 찾는 이름·필드가 구현에 존재하는지 재확인 (특히: 리소스 name vs Name 태그 구분, SA annotation 경로, deployment env 3개, nodeSelector 2키). 불일치 → 수정 후 재 validate.

- [ ] **Step 3: 하드코딩·보안 점검**

```powershell
Select-String -Path set-08/task-2/module-*/terraform/*.tf -Pattern "skills-" | Where-Object { $_.Line -notmatch "variable|default|description|#" }
```
Expected: 변수 default 외 하드코딩 없음 (k8s·eksctl 은 정적 파일 — NOTES 치환 범위 기록으로 커버). `0.0.0.0/0` 은 과제지 명시 위치(client in 80)만.

- [ ] **Step 4: NOTES 커버리지 표와 실제 파일 대조 + Commit**

```powershell
git add -A
git commit -m "Final review fixes for set-08 task-2 modules 2 and 4"
git log --oneline main..HEAD
```
Expected: Task 1~11 커밋 전부 존재. 수정 없으면 빈 커밋 생략.
