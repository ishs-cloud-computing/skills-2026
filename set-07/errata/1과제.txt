수정 내역

2026-07-31

0) (과제지) 직종협의회 회의 결과(aws-auth ConfigMap, access entry 등 강제 X)에 따라 8번 EKS - Security 파트의 아래 지문을 삭제합니다.
"클러스터의 Authentication mode는 EKS Access Entry를 사용하도록 하며, aws-auth ConfigMap 방식은 사용하지 않습니다."

1) (과제지) 과제지 11의 "와일드카드 액션 및 리소스는 사용해서는 안 됩니다." -> "와일드카드 액션은 사용해서는 안 됩니다."로 변경합니다. (리소스 와일드카드 제한 해제)

2) (채점지) 채점기준표의 채점 상의 유의 사항 18번으로 "채점기준표에는 Timestamp의 TZ가 UTC 기준이나, 채점 시 KST 기준으로 확인하도록 합니다"를 추가합니다.

3) (과제지) 10-3 WAF; "두 Rule Group의 Override action은 모두 None으로 두며, 별도의 rule-level override 없이 그대로 적용합니다."을 삭제 후, "필요할 경우 하기 Custom Response 지정을 위해 override를 진행할 수 있으며, 채점 시 XSS 공격을 진행함에 유의합니다."로 대체.

4) (과제지) 6번 Database 파트에서 "Lambda를 통한 예약 조회를 지원하기 위해" 부분 삭제.

5) (과제지) "CloudFront Distribution을 unicorn-svc-cf 이름으로 생성합니다." -> "CloudFront Distribution을 unicorn-svc-cf Comment를 가지도록 하여 생성합니다."로 변경.

6) (과제지) 11번 Security 항목. "와일드카드 액션은 사용해서는..." 문장 앞에 "Inline Policy를 사용하며, " 추가.

7) (채점지) 12-1-A 예상 출력; "출력값 형식이 위와 같고..." -> "출력된 로그가 위와 같고..."로 변경. 채점 시 유의사항 16번 참조하여 IP 등 값 무시하는 점 유지

8) (채점지) 6-3-A 예상 출력; "unicorn-book-app-svc" -> "unicorn-book-app-svc ClusterIP"로 변경하며, ClusterIP는 채점 시 유의사항 16번을 참고하여 파란색 글씨로 처리.

9) (채점지) (채점지) 채점 상의 유의 사항 19번으로 "source kubectl-connect 명령어를 실행할 때 오류가 발생할 경우, 선수가 원한다면 1회에 한해 rm -rf .kube/ 명령어를 통해 cache 등을 초기화할 수 있도록 합니다. 초기화 후 정상 작동한다면 채점을 진행할 수 있습니다."

10) (채점스크립트) 채점스크립트 9-2-A를 별첨 1로 변경하여 식별을 위한 고정 출력값을 삭제하도록 합니다.

11) (채점지) 12-1-A 예상 출력란 최하단에 아래 지침을 추가합니다. "** None 등의 값이 출력될 경우 무시하도록 합니다."

12) (채점지,채점스크립트) 채점스크립트 12-1-A의 date -u "+%Y-%m-%dT%H:%M:%SZ" 명령어를 TZ=Asia/Seoul date "+%Y-%m-%dT%H:%M:%S+09:00" 로 변경하여 KST 시간대로 출력될 수 있도록 합니다.

* 별첨 1 : 9-2-A 채점스크립트(mark.sh) 정정본
ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
ROLE_ARN=arn:aws:iam::$(echo $ACCOUNT_ID):role/unicorn-audit-role
aws sts assume-role --role-arn $ROLE_ARN --role-session-name mk 2>&1 | grep -oE AccessDenied | head -1
read -r AK SK TK < <(aws sts assume-role --role-arn $ROLE_ARN --role-session-name mk --external-id unicorn-audit-2026$number --query "Credentials.[AccessKeyId,SecretAccessKey,SessionToken]" --output text)
export AWS_ACCESS_KEY_ID=$AK AWS_SECRET_ACCESS_KEY=$SK AWS_SESSION_TOKEN=$TK
aws sts get-caller-identity --query Arn --output text
aws ec2 describe-vpcs --filters Name=tag:Name,Values=unicorn-vpc --query "Vpcs[0].VpcId" --output text
aws ec2 describe-instances 2>&1 | grep -oE "AccessDenied|UnauthorizedOperation" | head -1
unset AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_SESSION_TOKEN