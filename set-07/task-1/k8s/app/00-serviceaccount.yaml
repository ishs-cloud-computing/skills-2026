# Book App ServiceAccount (요구사항 8) — EKS Pod Identity 로 unicorn-book-app-role 에 매핑.
# (association 은 eksctl iam.podIdentityAssociations 에서 namespace+name 으로 연결)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: unicorn-book-app-sa
  namespace: unicorn
